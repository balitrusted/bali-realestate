export const metadata = {
  title: "Request Information",
  description: "Request information about a villa or property in Bali. Get in touch for viewings, rentals, or sales. We connect you with owners and agents.",
};

export default function RequestLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
